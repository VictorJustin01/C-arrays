#include <stdio.h>

int main(){
	
	int vet[8] = {10, 19, 40, 57, 90,1, 57, 6};
	int x, y;
	
	printf("Insira dois indices(de 0 ate 7 e separados por espaço) que deseja saber a soma dos valores: \n");
		scanf("%d %d", &x, &y);
		
		vet[x];
		vet[y];
			int soma = vet[x] + vet[y];
			printf("%d", soma);	
}
